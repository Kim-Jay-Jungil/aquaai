{
  "name": "aquaai",
  "private": true,
  "engines": { "node": "18.x" },
  "scripts": {
    "build": "mkdir -p public && cp -r index.html signin.html signup.html styles.css test.html test-api.html debug.html upload.html upload public/ 2>/dev/null || echo 'Build completed'",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.637.0",
    "@aws-sdk/s3-request-presigner": "^3.637.0",
    "@notionhq/client": "^2.2.14",
    "sharp": "^0.33.2",
    "@clerk/backend": "^1.7.0"
  },
  "devDependencies": {
    "vercel": "^33.5.1"
  }
}